[package]
name = "test-common"
version = "0.1.0"
authors = ["Andrew Plaza <andrew.plaza@parity.io>"]
edition = "2018"


[dependencies]
substrate-archive = { path = "../substrate-archive" }
substrate-archive-backend = { path = "../substrate-archive-backend" }
# test-runtime-client = { package = "substrate-test-runtime", git = "https://github.com/paritytech/substrate", branch = "insipx/add-tracing" }
# polkadot-runtime = { package = "polkadot-runtime", git = "https://github.com/paritytech/polkadot", branch = "master" }
sc-executor = { package = "sc-executor", git = "https://github.com/paritytech/substrate", branch = "insipx/add-tracing" }
# used in tests for storing test data on disk
dotenv = "0.15.0"
csv = "1"
anyhow = "1"
criterion = "0.3"
sp-io = { git = "https://github.com/paritytech/substrate", branch = "insipx/add-tracing" }
hex = "0.4"
tempfile = "3.2.0"
paste = "1"
